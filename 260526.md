# 📑 Arbeitsbericht (SYTB)


![Student](https://img.shields.io/badge/Student-Denis%20VELIC-lightblue?style=for-the-badge&logo=github)
![Klasse](https://img.shields.io/badge/Klasse-3AHITS-blue?style=for-the-badge&logo=googleclassroom&logoColor=white)
![Datum](https://img.shields.io/badge/Datum-26.05.2026-darkblue?style=for-the-badge&logo=googlecalendar&logoColor=white)

## 👤 Basisinformationen
| **Thema** | [Regular Expressions](https://www.franzmatejka.at/htl/doc/SYTB_3/15_if_ue.html)

| **Fach** | Systemtechnik (SYTB) 


---

## Theorie
**Kurze Zusammenfassung:**
In der Linux-Shell helfen `Regular Expressions`, Texte nach bestimmten Mustern zu durchsuchen oder zu verändern. Das Tool `grep` filtert damit Zeilen aus Texten heraus, während `sed` gefundene Muster direkt durch neuen Text ersetzt (Syntax: `s/Suchmuster/Ersatz/`). Da es im Standard-Regex keine erweiterten Zeichen wie das `(+)` für Wiederholungen gibt, nutzt man stattdessen das `(*)` oder baut logische Filter, indem man mehrere Befehle mit der `(|)` hintereinanderhängt.

**Wichtigsten Baustein:**
- `^` und `$`: `^` sucht am Zeilenanfang, `$` am Zeilenende. ^Fehler findet nur Zeilen, die mit "Fehler" beginnen
- `.` : Steht für genau ein beliebiges Zeichen
- `*` : Bedeutet, dass das Zeichen davor null- oder mehrmals vorkommen darf. `.*` steht für eine beliebig lange Zeichenkette.
- `[ ]` (Zeichenklasse): Sucht nach einem Zeichen aus der Klammer. `[0-9]` findet eine Ziffer, `[^0-9]` findet alles außer Ziffern.
- `\` : Schaltet die Sonderfunktion aus, um nach einem echten Punkt `(\.)` oder Sternchen `(\*)` zu suchen.

## 1. RegexOne 1 - 14

Arbeite die Lessons 1 - 14 durch
<img width="1119,33" height="525,4" alt="image" src="https://github.com/user-attachments/assets/87205ddf-cf37-48f2-8d3c-570d5335b0a8" />



## 2. Subdir Count

Schreibe einen shell Einzeiler mit dem man die Anzahl der Unterverzeichnisse im aktuellen Verzeichnisse zählt.

**shell:**
```sh
┌──(kali㉿kali)-[~]
└─$ ls
260526   Documents  Music     Public  Templates  Videos
Desktop  Downloads  Pictures  SYTB    Übungen
                                                                             
┌──(kali㉿kali)-[~]
└─$ ls -l | grep "^d" | wc -l
11

```
das `grep "^d"` filtert alles andere außer Directories raus, weil directories mit d beginnen 

## 3. Übung Directory
Ein Directory mit dem Namen `xyz` soll angelegt werden falls es nicht existiert. Im Directory lege eine Datei mit dem Namen `xyz.txt`.

**makedir.sh:**
```sh
#!/bin/bash

DIR=$1

# Schauen ob es noch nicht existiert 
if [ ! -d "$DIR" ]; then
    mkdir "$DIR"
    echo "$DIR" > "$DIR/$DIR.txt"

# Wenn es das verzeichnis schon gibt
else
    # Dateien zählen
    FILES=$(ls "$DIR" | wc -l)
    
    echo -n "Soll das Directory \"$DIR\" (mit $FILES Files) gelöscht werden? [j|n]: "
    read ANTWORT

    if [ "$ANTWORT" = "j" ]; then
        rm -r "$DIR"
        mkdir "$DIR"
        echo "$DIR" > "$DIR/$DIR.txt"
    fi
fi

```

**Ausgabe:**
```sh
./makedir.sh xyz
Soll das Directory "xyz" ( mit 1 Files ) gelöscht werden? [j|n]:
j
```

Hier wird am anfang geschaut ob es das Verzeichnis mit dem Namen schon exisitiert, falls nicht wir eins Erstellt und eine Text datei mit dem selben namen in dem Verzeichnis wird auch erstellt. Falls es doch keins gibt wird erst gezählt wie viele Files es in dem Ordner gibt und danach gefragt ob man das verzeichnis löschen will mit einer `[j|n]` abfrage. 

## 4. Übung Stundenplan V2
Schreibe ein Skript das aufgrund der aktuellen Zeit das aktuell Fach laut Stundenplan ausgibt. Pausen sind vereinfacht einem Fach zuzurechnen. Berücksichtige nur einen Wochentag, d.h. das Skript funktioniert nur an einem Donnerstag.

**splan2.sh:**
```sh
#!/bin/bash

# Die jetzige Zeit speichern 
JETZT=$(date +%-H%M)

echo -n "Donnerstag $(date +%H:%M) Uhr: "

# Mit der Stunde vergleichen

if [ $JETZT -gt 1510 ]; then
    echo "Schule aus"
elif [ $JETZT -gt 1420 ]; then
    echo "D"
elif [ $JETZT -gt 1325 ]; then
    echo "Mittagspause"
elif [ $JETZT -gt 1140 ]; then
    echo "BESP"
elif [ $JETZT -gt 1045 ]; then
    echo "ITP2PM"
elif [ $JETZT -gt 940 ]; then
    echo "SYTE"
elif [ $JETZT -gt 845 ]; then
    echo "AM"
elif [ $JETZT -gt 750 ]; then
    echo "NW2-p"
else
    echo "Schule noch nicht begonnen"
fi


```

**Ausgabe:**
```sh
./splanv2
Donnerstag 09:10 AM
```

`-gt` wie besprochen steht für `greater than`. Was das script hier macht er vergleicht die jetzige Zeit mit der Zeit von der Stunde am Donnerstag, das heißt wenn die Zeit größer als `7:50 = 750` hat die erste Stunde begonnen `NW2-P` usw.




